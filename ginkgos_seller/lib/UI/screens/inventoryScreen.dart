import 'package:flutter/material.dart';

class InventoryScreen extends StatelessWidget {
  static String id = 'InventoryScreen';
  const InventoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
